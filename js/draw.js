$(document).ready(function() {
  var chart = new fabric.Canvas('c', {
    containerClass: 'container well'
   // width: '100%'//,
    //border: '2px solid #000000'
  });

  chart.isDrawingMode = true;
  //chart.freeDrawingBrush.color="#000000";
  //chart.freeDrawingBrush.width=30;
});
